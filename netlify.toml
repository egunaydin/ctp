[build]
  base = "logistics-tracking-portal"   # Proje klasörün
  publish = "dist"                     # Build sonrası çıktı klasörü
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
